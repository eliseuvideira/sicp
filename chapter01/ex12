#!/usr/bin/env bb

(def numbers (map #(Integer/parseInt %) *command-line-args*))

(defn pascal [n k]
  (cond (= k 0) 1
        (= k n) 1
        :else (+ (pascal (- n 1) (- k 1)) (pascal (- n 1) k))))

(run! #(println (pascal (nth % 0) (nth % 1))) (partition 2 1 numbers))
